{
  "pluginAlias": "SleepysElite",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Sleepy's Elite"
      },
      "deviceNamePrefix": {
        "title": "Bed Bluetooth Name Prefix",
        "type": "string",
        "default": "Star25",
        "description": "Bluetooth advertising-name prefix used to discover the bed."
      }
    },
    "required": [
      "name",
      "deviceNamePrefix"
    ]
  },
  "form": [
    "name",
    "deviceNamePrefix"
  ]
}
